{
  "name": "react-restaurant",
  "scripts": {
    "start": "dotenv -e frontend/.env -- concurrently \"bun run --cwd frontend start\" \"dotnet run --project backend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "dotenv-cli": "^8.0.0"
  }
}
